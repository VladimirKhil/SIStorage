/** Provides a package restriction info. */
export default interface Restriction {
	/** Restriction identifier. */
	id: string;

	/** Restriction name. */
	name: string;

	/** Restriction value. */
	value: string;
}
