/** Defines packages sort direction. */
const enum PackageSortDirection {
	/** Ascending direction. */
	Ascending = 0,

	/** Descending direction. */
	Descending = 1,
}

export default PackageSortDirection;